version: '3'

services:
  node-app:
    image: node:18.17.0
    hostname: nextmap
    container_name: nextmap
    environment:
    # ${VAR_XXX} are required system env vars - add them to ~./bashrc
    - VIRTUAL_HOST=nextmap.${LETSENCRYPT_HOST}
    - LETSENCRYPT_HOST=nextmap.${LETSENCRYPT_HOST}
    - LETSENCRYPT_EMAIL=${LETSENCRYPT_EMAIL}
    expose:
      - "3000"
    volumes:
      - .:/app
      - ../package-mono/my-react-component/dist:/app/node_modules/my-react-component/
      - ../package-mono/my-react-component/node_modules:/app/node_modules/my-react-component/node_modules
    working_dir: /app
    # command: tail -f /dev/null
    command: "npm run dev"
    networks:
      main-net: 

networks:
  main-net:
    external:
      name: proxy_main-net